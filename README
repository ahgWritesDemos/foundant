Image Sharing Service

backend service written in golang
frontend static web site? simple react site?
persistence in postgres
file storage within docker volume
docker compose for DB, frontend, backend

Tests
  What needs testing?

API
  /images/
    list filenames
  /images/<id>
    yield image
  /images/<id>/metadata
    who, when, how big

Frontend:
  start with a static form, go from there

Questions / TODO
  Should I worry about huge images?
  What should it be called?

  on upload, redirect to the detail page of the new image
  maintain an in-memory list of images